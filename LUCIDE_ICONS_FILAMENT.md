# 🎨 Lucide Icons в Filament

## Что изменилось

Заменили текстовые поля для ввода иконок на визуальный селектор иконок Lucide в Filament.

## Установленный пакет

```bash
composer require guava/filament-icon-picker
```

**Пакет:** [guava/filament-icon-picker](https://github.com/GuavaCZ/filament-icon-picker) v2.3.1

## Было (текстовое поле)

```php
Forms\Components\TextInput::make('icon')
    ->label('Иконка (heroicon)')
    ->placeholder('heroicon-o-star')
```

**Проблемы:**
- ❌ Нужно знать название иконки
- ❌ Нужно помнить префикс (heroicon-o-, heroicon-s-)
- ❌ Нет предпросмотра
- ❌ Легко сделать опечатку
- ❌ Неудобно выбирать

## Стало (визуальный селектор)

```php
use Guava\FilamentIconPicker\Forms\IconPicker;

IconPicker::make('icon')
    ->label('Иконка')
    ->sets(['lucide'])
    ->default('star')
    ->columns(1)
```

**Преимущества:**
- ✅ Визуальный выбор из галереи
- ✅ Поиск по названию
- ✅ Предпросмотр иконки
- ✅ Lucide иконки вместо Heroicons
- ✅ Автокомплит
- ✅ Невозможно сделать опечатку

## Lucide vs Heroicons

### Heroicons
- Ограниченный набор (~300 иконок)
- Префиксы: `heroicon-o-`, `heroicon-s-`
- Стиль: минималистичный, простой
- Пример: `heroicon-o-star`, `heroicon-s-user`

### Lucide
- Большой набор (~1400 иконок)
- Префикс: просто название иконки
- Стиль: чистый, современный, универсальный
- Пример: `star`, `user`, `wifi`, `map-pin`
- Форк Feather Icons
- Активное развитие

## Где применено

### 1. EventResource - Особенности (features)

**Поле:** `features[].icon`

```php
Forms\Components\Repeater::make('features')
    ->schema([
        Forms\Components\TextInput::make('title')
            ->label('Заголовок')
            ->required(),
        
        Forms\Components\Textarea::make('description')
            ->label('Описание')
            ->rows(3),
        
        IconPicker::make('icon')
            ->label('Иконка')
            ->sets(['lucide'])
            ->default('star')
            ->columns(1),
    ])
```

**Использование:**
- Иконки для особенностей мероприятия
- Например: Wi-Fi, бассейн, питание, трансфер

### 2. TripResource - Удобства (amenities)

**Поле:** `amenities[].icon`

```php
Forms\Components\Repeater::make('amenities')
    ->label('Удобства в транспорте')
    ->schema([
        Forms\Components\TextInput::make('name')
            ->label('Название')
            ->required(),
        
        IconPicker::make('icon')
            ->label('Иконка')
            ->sets(['lucide'])
            ->default('wifi')
            ->columns(1),
    ])
```

**Использование:**
- Иконки для удобств в транспорте
- Например: Wi-Fi, кондиционер, розетки, туалет

## Настройки IconPicker

### Базовое использование
```php
IconPicker::make('icon')
    ->label('Иконка')
```

### С набором иконок
```php
IconPicker::make('icon')
    ->sets(['lucide'])  // Только Lucide иконки
```

### С значением по умолчанию
```php
IconPicker::make('icon')
    ->default('star')  // Иконка "звезда" по умолчанию
```

### С колонками
```php
IconPicker::make('icon')
    ->columns(1)  // Ширина поля (в grid)
```

### Доступные наборы иконок
```php
->sets([
    'lucide',      // Lucide Icons (рекомендуется)
    'heroicons',   // Heroicons
    'fontawesome', // Font Awesome (требует отдельной установки)
    'bootstrap',   // Bootstrap Icons
])
```

### Несколько наборов одновременно
```php
IconPicker::make('icon')
    ->sets(['lucide', 'heroicons'])  // Можно выбирать из обоих
```

### С поиском
```php
IconPicker::make('icon')
    ->searchable()  // Включено по умолчанию
```

### С обязательностью
```php
IconPicker::make('icon')
    ->required()
```

## Популярные Lucide иконки

### Общие
- `star` - звезда
- `heart` - сердце
- `check` - галочка
- `x` - крестик
- `info` - информация
- `alert-circle` - предупреждение

### Транспорт и путешествия
- `plane` - самолет
- `car` - машина
- `bus` - автобус
- `train` - поезд
- `map-pin` - метка на карте
- `compass` - компас
- `luggage` - багаж

### Удобства
- `wifi` - Wi-Fi
- `coffee` - кофе
- `utensils` - питание
- `bed` - кровать
- `bath` - ванна
- `air-conditioning` - кондиционер (но в Lucide это `wind`)
- `power` - розетка
- `tv` - телевизор

### Активности
- `dumbbell` - спорт
- `bike` - велосипед
- `waves` - плавание
- `mountain` - горы
- `camera` - фото
- `music` - музыка

## Интеграция с Frontend

В frontend (Nuxt) используйте те же названия иконок:

```vue
<template>
  <div v-for="feature in event.features" :key="feature.title">
    <!-- Lucide иконка -->
    <Icon :name="`lucide:${feature.icon}`" class="w-6 h-6" />
    <p>{{ feature.title }}</p>
  </div>
</template>
```

Или используйте `@iconify/vue`:

```vue
<script setup>
import { Icon } from '@iconify/vue'
</script>

<template>
  <Icon :icon="`lucide:${feature.icon}`" />
</template>
```

## Как использовать в Filament

1. **Откройте Filament Admin Panel:**
   ```
   http://api.camp.test/admin
   ```

2. **Перейдите в События или Поездки**

3. **Добавьте/Редактируйте запись**

4. **Найдите поле с иконкой:**
   - Откроется модальное окно с галереей иконок
   - Используйте поиск для быстрого нахождения
   - Кликните на иконку для выбора

5. **Сохраните**

## Примеры использования

### Пример 1: Особенности мероприятия
```
Заголовок: Wi-Fi
Описание: Бесплатный высокоскоростной интернет
Иконка: wifi
```

### Пример 2: Питание
```
Заголовок: Трёхразовое питание
Описание: Завтрак, обед и ужин включены
Иконка: utensils
```

### Пример 3: Транспорт
```
Заголовок: Трансфер
Описание: Встреча в аэропорту и доставка
Иконка: bus
```

### Пример 4: Удобства в автобусе
```
Название: Кондиционер
Иконка: wind
```

```
Название: USB-розетки
Иконка: usb
```

## Миграция существующих данных

Если у вас уже есть данные с Heroicons, нужно обновить названия:

```php
// Heroicons → Lucide
'heroicon-o-star' → 'star'
'heroicon-o-wifi' → 'wifi'
'heroicon-o-map-pin' → 'map-pin'
'heroicon-o-users' → 'users'
```

Можно создать скрипт миграции:

```php
// database/migrations/xxxx_migrate_icons_to_lucide.php
public function up()
{
    $events = Event::all();
    foreach ($events as $event) {
        if ($event->features) {
            $features = collect($event->features)->map(function ($feature) {
                if (isset($feature['icon'])) {
                    $feature['icon'] = str_replace(['heroicon-o-', 'heroicon-s-'], '', $feature['icon']);
                }
                return $feature;
            })->toArray();
            $event->update(['features' => $features]);
        }
    }
}
```

## Ссылки

- [Guava FilamentIconPicker](https://github.com/GuavaCZ/filament-icon-picker)
- [Lucide Icons](https://lucide.dev/)
- [Lucide Icons Gallery](https://lucide.dev/icons/)
- [Iconify для Vue](https://iconify.design/docs/icon-components/vue/)

## Результат

✅ **Визуальный выбор иконок** вместо текстового ввода
✅ **Lucide Icons** - современный набор из 1400+ иконок
✅ **Поиск и предпросмотр** для удобства
✅ **Автоматическая валидация** - невозможно выбрать несуществующую иконку
✅ **Применено в EventResource и TripResource**

**Теперь работать с иконками в Filament стало намного удобнее!** 🎨✨

